name     : PAIRED END READS
subtitle : none
tooltip  : Upload a PairedEndLibrary from FASTA/FASTQ file(s) into your Narrative.

screenshots :
    []

suggestions :
    apps:
        related :
            []
        next :
            []
    methods:
        related :
            []
        next :
            []

parameters :
    fastqFile1 :
        ui-name : READS FILE
        short-hint : Short read file containing a paired end library in FASTA/FASTQ format
        long-hint  : Short read file containing a paired end library in FASTA/FASTQ format

    fastqFile2 :
        ui-name : READS FILE
        short-hint : Optional second short read file containing a paired end library in FASTA/FASTQ format. Not needed in an interleaved library
        long-hint  : Optional second short read file containing a paired end library in FASTA/FASTQ format. Not needed in an interleaved library

    insertSizeMean :
        ui-name : MEAN INSERT SIZE
        short-hint : Specify the mean (average) insert length
        long-hint  : Specify the mean (average) insert length

    insertSizeStDev :
        ui-name : STDEV OF INSERT SIZE
        short-hint : Specify the standard deviation of insert lengths
        long-hint  : Specify the standard deviation of insert lengths

    readOrientationOutward :
        ui-name : READS ORIENTATION OUTWARD
        short-hint : Reads in a pair point outward
        long-hint  : Reads in a pair point outward

    outputObject :
        ui-name : OBJECT NAME
        short-hint : Provide a name for the output object that will be created by this upload
        long-hint  : Provide a name for the output object that will be created by this upload

description : none

technical-description : none
