#
# Define basic display information
#
name     : UPLOAD TRANSCRIPT
subtitle : |
    Import into your narrative a transcript object from a local file in FASTA format.
tooltip  : |
    Import into your narrative a transcript object from a local file in FASTA format.

screenshots :
    []

#
# Define the set of other narrative methods that should be suggested to the user.
#
suggestions :
    apps:
        related :
            []
        next :
            []
    methods:
        related :
            []
        next :
            []


#
# Configure the display and description of the parameters
#
parameters :
    fastaFile :
        ui-name : |
            FASTA FILE
        short-hint : |
            File with protein/cDNA in FASTA format (must have .fa or .fasta extension).
        long-hint  : |
            File with protein/cDNA in FASTA format (must have .fa or .fasta extension).

    outputObject :
        ui-name : |
            GENOME OBJECT NAME
        short-hint : |
            Provide a name for the genome object you are uploading.<br>ex) Athaliana.TAIR10
        long-hint  : |
            Provide a name for the genome object you are uploading.<br>ex) Athaliana.TAIR10

    genomeId :
        ui-name : |
            GENOME IDENTIFIER
        short-hint : |
            Optional genome identifier.
        long-hint  : |
            Optional genome identifier.

    dna :
        ui-name : |
            IS DNA
        short-hint : |
            Indicates that file contains nucleic acid sequences rather than proteins.
        long-hint  : |
            Indicates that file contains nucleic acid sequences rather than proteins.


description : |
    Import into your narrative a transcript object from a local file in FASTA format.


technical-description : |
    Import into your narrative a transcript object from a local file in FASTA format.
