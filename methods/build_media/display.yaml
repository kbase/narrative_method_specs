#
# Define basic display information
#
name     : Build Media
subtitle : Assemble a set of compounds to use as a media set for performing FBA on a metabolic model. [8]
tooltip  : Assemble a set of compounds to use as a media set for performing FBA on a metabolic model. [8]

screenshots :
    []

#
# Define the set of other narrative methods that should be suggested to the user.
#
method-suggestions :
    related :
        []
    next :
        []


#
# Configure the display and description of the parameters
#
parameters :
    param0 :
        ui-name : Media ID
        short-hint : Base media type [8.1]
        long-hint  : |
            Base media type [8.1]


description : |
    Assemble a set of compounds to use as a media set for performing FBA on a metabolic model. [8]


technical-description : |
    Assemble a set of compounds to use as a media set for performing FBA on a metabolic model. [8]
